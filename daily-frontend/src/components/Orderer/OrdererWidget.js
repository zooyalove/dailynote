import React from 'react';

const OrdererWidget = ({children}) => {
    return (
        <div className="orderer-widget">
            {children}
        </div>
    );
};

export default OrdererWidget;
{
    "scripts": {
        "build": "babel src -d build --presets=es2015",
        "start": "cross-env NODE_ENV=production node ./build/main.js",
        "development": "cross-env NODE_ENV=development node ./scripts/development.js"
    }
}